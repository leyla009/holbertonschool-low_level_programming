#include <stdio.h>
int main(void)
{
    int n;
    int a[5];
    int *p;
    /*
     * write your line of code here...
     * Remember:
     * - you are not allowed to use a
     * - you are not allowed to modify p
     * - only one statement
     * - you are not allowed to code anything else than this line of code
     */
    *(p + 2) = 98;

    /*
     * You can add extra comments or blank lines if needed
     * to reach the total of 23 lines
     *
     */
    return (0);
}
