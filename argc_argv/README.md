# argc_argv

This directory contains C programs that demonstrate the use of **command line arguments** (`argc` and `argv`) in C.

## Files

### 0-whatsmyname.c

**Description:**  
A program that prints its own name.  

**Usage:**  
```bash
$ gcc -Wall -pedantic -Werror -Wextra -std=gnu89 0-whatsmyname.c -o mynameis
$ ./mynameis
./mynameis

